
<% if current_user.id == @post.user_id %>

  <div class="page-header">
    <h1>Editing post</h1>
  </div>

  <%= render 'form' %>

  <%= link_to 'Show', @post %> |
  <%= link_to 'Back', posts_path %>

<% end %>
